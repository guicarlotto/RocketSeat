/* 

Object - Objeto
         tem Propriedades
         tem Funcionalidades ou Métodos

*/

console.log({
    nome:"Gui",
    idade: 36,
    andar: function() {console.log('andar')}
})

const person= {
    name: 'Gui',
    age: 30,
    weight: 54.5,
    isAdmin: true,
}

console.log(`${person.name} tem ${person.age} anos.
Pesa - ${person.weight} Kgs.
É admin: ${person.isAdmin}.`)